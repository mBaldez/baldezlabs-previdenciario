name: Análise Semanal de Mercado

# Workflow mantido para referência futura (Fase 2 — Meta Graph API)
# A análise atualmente roda localmente via scripts/rodar_analise_local.bat
# e o resultado é enviado manualmente ao GitHub todo domingo.

on:
  workflow_dispatch:  # Apenas manual — sem cron ativo por enquanto

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Análise rodando localmente. Ver scripts/rodar_analise_local.bat"
